// Local components
import Layout from '@/lib/frontend/layout'
import WeatherSearchBar from '@/lib/frontend/searchBar'

const WeatherHome = () => {
  return (
    <Layout>
      <WeatherSearchBar/>
    </Layout>
  )
}

export default WeatherHome;
